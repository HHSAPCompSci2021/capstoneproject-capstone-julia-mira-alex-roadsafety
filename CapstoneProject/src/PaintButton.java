import mhaldar.shapes.Shape;
import screens.*; 
public class PaintButton extends Button{
	Window w; 
	public PaintButton(Shape button, String text, int[] color, Paint p) {
		super(button, text, color); 
	}
}
