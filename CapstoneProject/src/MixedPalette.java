import java.awt.Color; 
/**
 * a palette based off of one color, contains shades 
 * 
 *
 */
public class MixedPalette extends Palette{
	Paint[] shades;
	/**
	 * creates a palette with paints based off of root color c 
	 * @param c
	 */
	public MixedPalette(Color c ) {
		
	}
}
