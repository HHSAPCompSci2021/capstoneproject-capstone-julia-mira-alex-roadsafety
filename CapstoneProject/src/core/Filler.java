package core;
import processing.core.*;
import java.awt.Color; 
import java.awt.Point; 

public class Filler {
	PGraphics g; 
	public Filler(PGraphics pg) {
		g = pg; 
	}
	public void fill(Point p, Color c) {
		double x = p.getX(); 
		double y = p.getY(); 
	//copy n paste the fill thing	if()
	}
}
